import 'package:flutter/material.dart';

class KitePages extends StatelessWidget {

  const KitePages({ Key? key }) : super(key: key);

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text(''),),
           body: Container(),
       );
  }
}
