// -----------------------------------------------------------------------------
// GENERATED BY KITE.
//
// Do not edit this file manually.
// Update routing through Kite CLI or kite.yaml.
//
// https://kite.orkitt.dev
// -----------------------------------------------------------------------------

import 'package:go_router/go_router.dart';

import '{{branch.screenImport}}';
import '../../app_routes.dart';
{{branch.childImports}}
final StatefulShellBranch {{branch.variableName}} = StatefulShellBranch(
  routes: <RouteBase>[
    GoRoute(
      path: AppRoutes.{{branch.routeConstant}},
      builder: (context, state) => const {{branch.screenClass}}(),
      routes: <RouteBase>[
{{branch.children}}      ],
    ),
  ],
);

String _relativeChildPath(String parentPath, String childPath) {
  if (parentPath == '/') {
    return childPath.substring(1);
  }
  return childPath.substring(parentPath.length + 1);
}
